[project]
name = "trialscribe-streamlit"
version = "0.1.0"
description = "TrialScribe interim Streamlit UI"
requires-python = ">=3.12"
dependencies = [
    "streamlit>=1.45.1",
    "trialscribe-ai",
]

[tool.uv.sources]
trialscribe-ai = { path = "../../backend/services/ai-engine", editable = true }
