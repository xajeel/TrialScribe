# Requires Docker Compose 2.24.4 or newer for the !reset tag.
services:
  postgres:
    ports: !reset []

  redis:
    ports: !reset []

  kafka:
    ports: !reset []

  chroma:
    ports: !reset []

  prometheus:
    ports: !reset []

  grafana:
    ports: !reset []

  postgres-exporter:
    ports: !reset []

  redis-exporter:
    ports: !reset []
