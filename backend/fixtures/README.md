# fixtures

Sample trial-data files used for local development and manual testing of the
[ai-engine](../services/ai-engine) upload flow:

- `test-final.json`, `output.json` — example trial-design JSON payloads for `/upload-json`.
- `sample_study_document.pdf` — an example supporting document for `/upload-documents`.

Not consumed by any code path — these are development-time inputs only.
