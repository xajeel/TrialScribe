[project]
name = "trialscribe-monorepo"
version = "0.1.0"
description = "TrialScribe — AI clinical trial protocol platform (monorepo)"
requires-python = ">=3.12"
dependencies = []

[dependency-groups]
dev = ["ruff>=0.4", "pytest>=8"]

[tool.uv.workspace]
members = ["backend/services/ai-engine", "frontend/streamlit-ui"]
