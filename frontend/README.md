# frontend

The supported TrialScribe UI is the React application in [web](web). Start it with
`./scripts.sh run web` during local development, or as part of
`./scripts.sh release up` for the full Compose stack.
